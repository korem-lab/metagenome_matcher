path to input files:<full path to mpileups>/*.pileup.gz
path to temporary compute space:<full path to temp folder>
path to output folder:<full path to destination folder with final output>
self, all, or path to pairs file:all
path to input genotype PLINK fileset:<full path to PLINK fileset>/<basename of .bed, .bim, .fam files>
sequencing quality threshold (as Phred score):36
alignment quality threshold (as Phred score):36
job ID:<name of job, will be used to name output folder>
graph?:<yes or no>
