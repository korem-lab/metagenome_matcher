path to input files:<full path to mpileup files>/*.pileup.gz
path to temporary compute space (exclude final slash):<full path to temp folder>
path to output folder (exclude final slash):<full path to destination folder for output>
"all", only samples from "same" donor, path to pairs file, or only samples from donors not in .fam file (path to .fam):all
sequencing quality threshold (as Phred score):36
alignment quality threshold (as Phred score):36
job ID:<name for job, will be used to name output folder>
graph:<yes or no>
